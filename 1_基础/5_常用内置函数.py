""" 
dir(obj) 返回指定对象的成员列表

eval()  计算并返回字符串s中表达式的值  类似于 js中的eval

int(x[,d]) 如果x是数值类型 那么返回x整数部分 如果x是字符串类型 那么根据d进制转10进制 d默认为10进制 
          如果不能x不能转为10进制则报错
"""

x = {'x' : 1 , 'y ': 2}
# print(dir(x))
# print(dir())

help(dir)